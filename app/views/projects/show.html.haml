#main_container
  .col-md-2
    = render :partial => 'clients/client_block', :locals => {:client => current_client}

  .col-md-8
    = render :partial => 'projects/show', :locals => {:project => @project, :tasks => @tasks}